{
  "name": "vibesphere-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    // Add your backend dependencies here, e.g.,
    // "express": "^4.18.2"
  },
  "devDependencies": {
    // Optionally add development dependencies, e.g.,
    // "nodemon": "^3.0.0"
  }
}
